server:
  port: 11003
  servlet:
    context-path: /rSpace-job
spring:
  application:
    name: app
  profiles:
    active: "@profiles.active@"
    group:
      "dev": "dev,db-dev,domain-dev"
task:
  enabled: true